mutation CreateExamDraftVariantsMutation(
  $sourceQuestionId: ID!
  $totalVariants: Int!
) {
  createExamDraftVariants(
    sourceQuestionId: $sourceQuestionId
    totalVariants: $totalVariants
  ) {
    id
    title
    prompt
    type
    difficulty
    options
    correctAnswer
    tags
  }
}
