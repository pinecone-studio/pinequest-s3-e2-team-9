mutation RecordAttemptIntegrityEvent(
  $attemptId: ID!
  $type: AttemptIntegrityEventType!
  $details: String
) {
  recordAttemptIntegrityEvent(
    attemptId: $attemptId
    type: $type
    details: $details
  )
}
