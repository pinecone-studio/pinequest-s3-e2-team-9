query QuestionBankDetailQuery($id: ID!) {
  me {
    id
  }
  questionAccessRequests {
    id
    status
    createdAt
    reviewedAt
    requester {
      id
      fullName
    }
    owner {
      id
      fullName
    }
    question {
      id
      title
      prompt
      bank {
        id
        title
      }
    }
  }
  questionBanks {
    id
    title
    grade
    subject
    topic
    owner {
      id
    }
  }
  questionBank(id: $id) {
    id
    title
    description
    grade
    subject
    topic
    topics
    visibility
    questionCount
    owner {
      id
      fullName
    }
    questions {
      id
      title
      prompt
      type
      difficulty
      shareScope
      requiresAccessRequest
      options
      correctAnswer
      tags
      createdBy {
        id
        fullName
      }
    }
  }
}
